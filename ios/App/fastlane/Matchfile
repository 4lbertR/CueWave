# Matchfile for iOS code signing via Fastlane match

git_url("https://github.com/4lbertR/YOUR_CERTIFICATES_REPO.git") # <-- Replace with your certificates repo

type("appstore") # or "development", "adhoc", "enterprise" if needed

app_identifier(["com.your.bundle.id"]) # <-- Replace with your app’s bundle identifier

username("your-apple-id@email.com") # <-- Replace with your Apple developer account email

# Optionally, specify the branch for your certs repo
# git_branch("master")

# Optionally, specify the team id if you have multiple Apple teams
# team_id("XXXXXXXXXX")